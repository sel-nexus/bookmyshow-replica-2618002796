import React from 'react';

/** Displays the non-interactive confirmation interval before one booking submission. */
export function ProcessingView() {
  return <p role="status" aria-live="polite">Processing Payment...</p>;
}
